version: '2'
services:
  gestionformation-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/gestionFormation/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=gestionFormation
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
